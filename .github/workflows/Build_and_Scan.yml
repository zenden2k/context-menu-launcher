name: "🚀 Build, Microsoft Defender Check & Verification"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  SOLUTION_FILE_PATH: .

jobs:
  # --- JOB 1: WINDOWS BUILD & LOCAL AUDIT ---
  build-windows:
    name: "🏗️ Build & Win-Audit (Win x86 🪟)"
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: ⚠️ Legal & Safety Notice
      shell: pwsh
      run: |
        "### 💀 CRITICAL SYSTEM NOTICE" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "> [!CAUTION]" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "> **USER BEWARE:** This autobuild is supplied without warranty. If the world ends, **it's not our fault.**" | Add-Content -Path $env:GITHUB_STEP_SUMMARY

    - name: ➕ Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v2

    - name: 🏗️ NuGet Restore & Build
      run: |
        nuget restore ${{env.SOLUTION_FILE_PATH}}
        msbuild /m:8 /p:Configuration=Release ${{env.SOLUTION_FILE_PATH}}
        msbuild /m:8 /p:Configuration=ReleaseHidden ${{env.SOLUTION_FILE_PATH}}

    - name: 🔍 Integrity Audit (SHA-256)
      shell: pwsh
      run: |
        $exe1 = "Release\singleinstance.exe"
        $exe2 = "Release\singleinstance_hidden.exe"
        $h1 = (Get-FileHash $exe1 -Algorithm SHA256).Hash.ToLower()
        $h2 = (Get-FileHash $exe2 -Algorithm SHA256).Hash.ToLower()
        
        # Write checksums for Linux (hash + space + space + filename)
        "$h1  singleinstance.exe" | Out-File -FilePath Release\checksums.txt -Encoding ascii
        "$h2  singleinstance_hidden.exe" | Add-Content -Path Release\checksums.txt

        "### 🔒 Binary Integrity Report" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "| File | SHA-256 Checksum |" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "| :--- | :--- |" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "| **singleinstance.exe** | ``$h1`` |" | Add-Content -Path $env:GITHUB_STEP_SUMMARY
        "| **singleinstance_hidden.exe** | ``$h2`` |" | Add-Content -Path $env:GITHUB_STEP_SUMMARY

    - name: 🔍 Deep PE Audit (YARA Win64)
      shell: pwsh
      run: |
        Invoke-WebRequest -Uri "https://github.com/VirusTotal/yara/releases/download/v4.5.2/yara-v4.5.2-2326-win64.zip" -OutFile "yara.zip"
        Expand-Archive -Path "yara.zip" -DestinationPath "yara-bin"
        $rule = "import 'pe'`nrule Win32_Executable_Audit { condition: uint16(0) == 0x5A4D and pe.subsystem == pe.SUBSYSTEM_WINDOWS_GUI }"
        $rule | Out-File -FilePath "audit.yar" -Encoding ascii
        ./yara-bin/yara64.exe -m audit.yar Release/singleinstance.exe
        ./yara-bin/yara64.exe -m audit.yar Release/singleinstance_hidden.exe
        "✅ Deep PE Audit Complete (YARA 4.5.2)" | Add-Content -Path $env:GITHUB_STEP_SUMMARY

    - name: 📤 Upload Binaries for Linux Scan
      uses: actions/upload-artifact@v4
      with:
        name: build-binaries
        path: |
          Release/*.exe
          Release/checksums.txt

  # --- JOB 2: LINUX MULTI-ENGINE SCAN ---
  security-scan:
    name: "🛡️ Security Audit (Linux 🐧)"
    needs: build-windows
    runs-on: ubuntu-latest
    steps:
    - name: 📥 Download Binaries
      uses: actions/download-artifact@v4
      with:
        name: build-binaries
        path: scan-dir

    - name: 🔍 Verify Transfer Integrity
      working-directory: scan-dir
      run: |
        echo "### 📂 Cross-Platform Integrity Check" >> $GITHUB_STEP_SUMMARY
        if sha256sum -c checksums.txt; then
          echo "✅ Integrity check passed." >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ Integrity check failed!" >> $GITHUB_STEP_SUMMARY
          exit 1
        fi
        echo "---" >> $GITHUB_STEP_SUMMARY

    - name: 🛡️ ClamAV & YARA Scan
      uses: hugoalh/scan-virus-ghaction@v0.20.1
      with:
        clamav_enable: true
        yara_enable: true
        found_summary: true
        statistics_summary: true
        ignores_post: |
          $input.Symbol -match 'extension' -or $input.Symbol -match 'filename'

    - name: 🛡️ Microsoft Defender Scan (Linux)
      uses: jbrotsos/jbro-test-ghaction@v1
      with:
        args: "scan --path ./scan-dir"

    - name: 🚀 Final Artifact Release
      uses: actions/upload-artifact@v4
      with:
        name: singleinstance-final-release
        path: scan-dir/*

    - name: 🏁 Final Clearance
      run: |
        echo "### ✅ Security Checks Passed" >> $GITHUB_STEP_SUMMARY
        echo "> [!IMPORTANT]" >> $GITHUB_STEP_SUMMARY
        echo "> Binaries verified via **Microsoft Defender**, **ClamAV**, **YARA (Win/Linux)**, and **SHA-256 Checksums**." >> $GITHUB_STEP_SUMMARY
